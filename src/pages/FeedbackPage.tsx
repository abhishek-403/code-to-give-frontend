import React from 'react';
import FeedbackForm from '@/components/feedback-form'; // Keep the form logic in components

const FeedbackPage: React.FC = () => {
  return <FeedbackForm />;
};

export default FeedbackPage;